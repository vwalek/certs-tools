## My custom certificate scripts 

### The "gatessl"

The gatessl is just a wrapper around openssl to list multiple certificates from one file.  
More info at [gatessl](./gatessl/).

### The "generate-certs"